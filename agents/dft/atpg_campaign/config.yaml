# Fleet manifest for the ATPG Campaign agent (atpg_campaign).
# Parsed by EdaAgent.read_spec / gateway YAML scanner. Comments are documentation only.

# Fleet schema version for the agent manifest parser.
apiVersion: agentfleet/v1
# Resource kind; must be Agent for gateway YAML scanning.
kind: Agent
# Human- and machine-readable identity for dashboards and fleet select.
metadata:
  # Stable agent id used in delegates_to, tool agent_id, and routing.
  name: atpg_campaign
  # UI label shown in the dashboard and logs.
  display_name: ATPG Campaign Agent
  # One-paragraph charter for planners and operators.
  description: Generate and analyze deterministic pattern campaigns across the required fault models.
  # Stage and role for this agent.
  labels:
    # Pipeline stage this agent owns or serves.
    stage: dft
    # Role class: lead, specialist, or validator.
    role: worker
# Role class: lead, specialist, or validator.
role: worker
# Pipeline stage this agent owns or serves.
stage: dft
# Normative statement of what this agent owes the program.
responsibility: Generate and analyze deterministic pattern campaigns across the required fault models.
# Hard may / may_not envelope enforced in planning and reviews.
boundary:
  # Actions this agent is allowed to take on candidates and memory.
  may:
  # List entry consumed by the atpg_campaign manifest: Submit an ATPG job
  - Submit an ATPG job
  # List entry consumed by the atpg_campaign manifest: Write pattern budgets
  - Write pattern budgets
  # List entry consumed by the atpg_campaign manifest: Publish fault coverage, pattern count, and test power
  - Publish fault coverage, pattern count, and test power
  # Forbidden actions (waivers, baseline promotion, self-signoff, etc.).
  may_not:
  # List entry consumed by the atpg_campaign manifest: Drop a fault model from the contract
  - Drop a fault model from the contract
  # List entry consumed by the atpg_campaign manifest: Edit RTL
  - Edit RTL
  # List entry consumed by the atpg_campaign manifest: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # List entry consumed by the atpg_campaign manifest: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # List entry consumed by the atpg_campaign manifest: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Child agent ids this lead may open workflows for.
delegates_to: []
# Independent gate agents that must grade this agent's output.
validators: []
# How other services reach this agent's HTTP process.
connection:
  # Hostname from sibling containers (Docker Desktop bridge).
  host: host.docker.internal
  # TCP port exposed by this agent's Dockerfile / server.
  port: 8227
  # HTTP paths the AgentService registers.
  endpoints:
    # Liveness probe path for orchestrators.
    health: /health
    # Task execution POST path used by the fleet server.
    execute: /tasks/execute
# EDA tool backend binding; none until a framework adapter is attached.
backend:
  # Backend adapter type (none means tool_observation returns not_run).
  type: none
# How tools are invoked inside the agent process.
execution:
  # Functional or test-mode filter for report reads.
  mode: direct_function
  # Parallelism limits for in-flight tasks.
  concurrency:
    # Maximum simultaneous tasks for this specialist.
    max_tasks: 1
# Timeouts and retry policy for failed DFT/lab work.
reliability:
  # Wall-clock seconds before a task is abandoned.
  task_timeout_secs: 120
  # Automatic retries before on_failure policy applies.
  max_retries: 1
  # Planner reaction when the specialist fails (e.g. replan).
  on_failure: replan
# Advertised capability ids for planners (may mirror skills).
capabilities:
# Manifest field '- id' for atpg_campaign (dft).
- id: write_fault_model_list
  # One-paragraph charter for planners and operators.
  description: Write the fault models this campaign must cover. Do not omit a required model.
# Manifest field '- id' for atpg_campaign (dft).
- id: write_pattern_budget
  # One-paragraph charter for planners and operators.
  description: Write the pattern-count and tester-time budget.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_stuck_at_patterns
  # One-paragraph charter for planners and operators.
  description: Generate stuck-at patterns.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_transition_patterns
  # One-paragraph charter for planners and operators.
  description: Generate transition-delay patterns.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_path_delay_patterns
  # One-paragraph charter for planners and operators.
  description: Generate path-delay patterns for the listed paths.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_iddq_patterns
  # One-paragraph charter for planners and operators.
  description: Generate IDDQ patterns when the contract requires them.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Read coverage, untestable faults, and aborted faults by model.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_pattern_count
  # One-paragraph charter for planners and operators.
  description: Read pattern count and estimated tester time.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_test_power
  # One-paragraph charter for planners and operators.
  description: Read shift and capture power.
# Manifest field '- id' for atpg_campaign (dft).
- id: compare_coverage_to_requirement
  # One-paragraph charter for planners and operators.
  description: Compare each fault model with its required coverage.
# Manifest field '- id' for atpg_campaign (dft).
- id: analyze_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Publish coverage, pattern count, tester time, and test power.
# Manifest field '- id' for atpg_campaign (dft).
- id: flag_dropped_fault_model
  # One-paragraph charter for planners and operators.
  description: Publish a campaign that omitted a required fault model.
# Manifest field '- id' for atpg_campaign (dft).
- id: flag_test_power_over_budget
  # One-paragraph charter for planners and operators.
  description: Publish shift or capture power above the budget.
# Manifest field '- id' for atpg_campaign (dft).
- id: record_atpg_tool_version
  # One-paragraph charter for planners and operators.
  description: Record the ATPG tool, version, and fault-model settings.
# Concrete tool bindings: module, callable, action, params.
skills:
# Manifest field '- id' for atpg_campaign (dft).
- id: write_fault_model_list
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_fault_model_list
  # One-paragraph charter for planners and operators.
  description: Write the fault models this campaign must cover. Do not omit a required model.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for atpg_campaign (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for atpg_campaign (dft).
- id: write_pattern_budget
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_pattern_budget
  # One-paragraph charter for planners and operators.
  description: Write the pattern-count and tester-time budget.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for atpg_campaign (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_stuck_at_patterns
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: generate_stuck_at_patterns
  # One-paragraph charter for planners and operators.
  description: Generate stuck-at patterns.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for atpg_campaign (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_transition_patterns
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: generate_transition_patterns
  # One-paragraph charter for planners and operators.
  description: Generate transition-delay patterns.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for atpg_campaign (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_path_delay_patterns
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: generate_path_delay_patterns
  # One-paragraph charter for planners and operators.
  description: Generate path-delay patterns for the listed paths.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for atpg_campaign (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for atpg_campaign (dft).
- id: generate_iddq_patterns
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: generate_iddq_patterns
  # One-paragraph charter for planners and operators.
  description: Generate IDDQ patterns when the contract requires them.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for atpg_campaign (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for atpg_campaign (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_fault_coverage
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Read coverage, untestable faults, and aborted faults by model.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_pattern_count
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_pattern_count
  # One-paragraph charter for planners and operators.
  description: Read pattern count and estimated tester time.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for atpg_campaign (dft).
- id: read_test_power
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_test_power
  # One-paragraph charter for planners and operators.
  description: Read shift and capture power.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for atpg_campaign (dft).
- id: compare_coverage_to_requirement
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: compare_coverage_to_requirement
  # One-paragraph charter for planners and operators.
  description: Compare each fault model with its required coverage.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for atpg_campaign (dft).
- id: analyze_fault_coverage
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: analyze_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Publish coverage, pattern count, tester time, and test power.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for atpg_campaign (dft).
- id: flag_dropped_fault_model
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_dropped_fault_model
  # One-paragraph charter for planners and operators.
  description: Publish a campaign that omitted a required fault model.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for atpg_campaign (dft).
- id: flag_test_power_over_budget
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_test_power_over_budget
  # One-paragraph charter for planners and operators.
  description: Publish shift or capture power above the budget.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for atpg_campaign (dft).
- id: record_atpg_tool_version
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: record_atpg_tool_version
  # One-paragraph charter for planners and operators.
  description: Record the ATPG tool, version, and fault-model settings.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for atpg_campaign (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for atpg_campaign (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Local and shared engineering-memory configuration.
memory:
  # Named memory stores this agent may touch.
  stores:
  # Manifest field '- id' for atpg_campaign (dft).
  - id: journal
    # One-paragraph charter for planners and operators.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Backend adapter type (none means tool_observation returns not_run).
    type: list
    # Where the store is kept (file vs ephemeral).
    persistence: file
    # Store-specific configuration block.
    config:
      # Filesystem root for silicon memory (env-substitutable).
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry stream configuration.
observability:
  # Seconds between liveness heartbeats.
  heartbeat_interval_secs: 5
  # Where skill_calls and artifacts are ingested.
  telemetry:
    # Telemetry service base URL (TELEMETRY_URL).
    endpoint: ${TELEMETRY_URL}
    # Named high-level telemetry stream categories.
    streams:
    # Manifest field '- name' for atpg_campaign (dft).
    - name: skill_calls
      # One-paragraph charter for planners and operators.
      description: Tool invocations.
    # Manifest field '- name' for atpg_campaign (dft).
    - name: artifacts
      # One-paragraph charter for planners and operators.
      description: Structured task results.
# Container image and Dockerfile pointers for compose.
deployment:
  # Published image tag for this agent.
  image: agentfleet/atpg_campaign:1.0
  # Relative Dockerfile path used to build the image.
  dockerfile: Dockerfile
  # Env vars injected into the container.
  environment:
    # Optional bound EDA/lab framework adapter name.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Context assembly policy for engineering memory.
context:
  # Record kinds always preferred into the context package.
  include:
  # List entry consumed by the atpg_campaign manifest: requirements
  - requirements
  # List entry consumed by the atpg_campaign manifest: canonical_source
  - canonical_source
  # List entry consumed by the atpg_campaign manifest: open_findings
  - open_findings
  # List entry consumed by the atpg_campaign manifest: gates
  - gates
  # Record kinds filtered out before ranking.
  exclude:
  # List entry consumed by the atpg_campaign manifest: stale_candidates
  - stale_candidates
  # Validation states removed before context ranking.
  drop:
  # List entry consumed by the atpg_campaign manifest: rejected
  - rejected
  # Most-trusted authorship/validation states first.
  precedence:
  # List entry consumed by the atpg_campaign manifest: human-authored
  - human-authored
  # List entry consumed by the atpg_campaign manifest: validated
  - validated
  # List entry consumed by the atpg_campaign manifest: provisional
  - provisional
  # States never dropped solely for token budget.
  protect:
  # List entry consumed by the atpg_campaign manifest: human-authored
  - human-authored
  # Approximate token cap for the assembled context package.
  token_budget: 80000
