# Python dependencies for the Assertion/Formal Agent container / local venv.
# HTTP and config stack only; does not install a simulator, formal engine, or UVM library.
# Agent role: writes SVA assertions, assumptions, and cover properties and drives formal proof campaigns.

fastapi  # ASGI web framework exposing /health and /tasks/execute for this specialist.
uvicorn[standard]  # ASGI server that runs the FastAPI app in Docker or locally.
pydantic  # Request/response and AgentSpec validation used by AgentService.
PyYAML  # Parses this agent's config.yaml fleet metadata and skill bindings.
