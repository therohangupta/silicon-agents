# Agent manifest for Macro-Placement Experiment Worker (role=worker, stage=floorplan, port=8239).
# EDA focus: Hard-macro placement under halo, channel, orientation, keepouts; OpenROAD macro placement.
# Every mapping key and list entry has a comment; multi-line string values are untouched.

# Fleet schema version for this Agent manifest (agentfleet/v1).
apiVersion: agentfleet/v1
# Resource kind; Agent means a runnable chip-design agent service.
kind: Agent
# Human- and registry-facing identity for this agent.
metadata:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  name: macro_placement
  # UI-facing title for operators and dashboards.
  display_name: Macro-Placement Experiment Worker
  # One-paragraph responsibility statement shown in registries and UIs.
  description: 'Generate and score one isolated macro placement under halo, channel, orientation, and
    connectivity limits. Adapter target: OpenROAD initialize_floorplan and macro placement.'
  # Labels expose stage and role.
  labels:
    # Pipeline stage this agent belongs to in the backend flow.
    stage: floorplan
    # lead orchestrates workers; worker executes specialist tools.
    role: worker
# lead orchestrates workers; worker executes specialist tools.
role: worker
# Pipeline stage this agent belongs to in the backend flow.
stage: floorplan
# Authoritative responsibility text mirrored from the design system.
responsibility: 'Generate and score one isolated macro placement under halo, channel, orientation, and
  connectivity limits. Adapter target: OpenROAD initialize_floorplan and macro placement.'
# Hard may / may_not rules the runtime enforces on tool actions.
boundary:
  # Actions this agent is allowed to perform.
  may:
  # Entry under `may` for Macro-Placement Experiment Worker: Write one macro placement candidate
  - Write one macro placement candidate
  # Entry under `may` for Macro-Placement Experiment Worker: Submit a legality check
  - Submit a legality check
  # Actions this agent must never perform (promotion, waivers, etc.).
  may_not:
  # Entry under `may_not` for Macro-Placement Experiment Worker: Edit the shared physical database
  - Edit the shared physical database
  # Entry under `may_not` for Macro-Placement Experiment Worker: Move a macro into a keepout
  - Move a macro into a keepout
  # Entry under `may_not` for Macro-Placement Experiment Worker: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # Entry under `may_not` for Macro-Placement Experiment Worker: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # Entry under `may_not` for Macro-Placement Experiment Worker: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Child agent ids this lead may open workflows toward.
delegates_to: []
# Optional validator agent ids that must confirm outcomes.
validators: []
# How other services reach this agent's HTTP process.
connection:
  # Hostname inside Docker networks (host.docker.internal for local).
  host: host.docker.internal
  # TCP port this agent's uvicorn server listens on.
  port: 8239
  # Relative HTTP paths for health and task execution.
  endpoints:
    # Liveness/readiness path probed by the fleet.
    health: /health
    # Path that accepts AgentTaskRequest payloads.
    execute: /tasks/execute
# Tool-backend binding; type none means observations are not_run until bound.
backend:
  # Backend adapter type (none until OpenROAD/Yosys/etc. is wired).
  type: none
# How the agent runs tools for a task.
execution:
  # direct_function invokes Python callables named in skills.
  mode: direct_function
  # Limits on parallel task handling inside one process.
  concurrency:
    # Maximum simultaneous tasks (1 keeps experiment isolation simple).
    max_tasks: 1
# Timeouts, retries, and failure policy for task handling.
reliability:
  # Wall-clock seconds before a task is considered failed.
  task_timeout_secs: 120
  # How many times the fleet may retry a failed task.
  max_retries: 1
  # Policy when tools fail (replan asks the lead to adjust the graph).
  on_failure: replan
# Declared capability ids surfaced to planners (mirrors skills).
capabilities:
# Stable identifier for this list entry.
- id: initialize_floorplan
  # Human-readable description for this manifest entry.
  description: 'Create the die, core, rows, and sites for this candidate. Adapter target: OpenROAD initialize_floorplan.'
# Stable identifier for this list entry.
- id: read_macro_inventory
  # Human-readable description for this manifest entry.
  description: Read macros, abstracts, and orientation limits.
# Stable identifier for this list entry.
- id: read_floorplan_keepouts
  # Human-readable description for this manifest entry.
  description: Read blockages, halos, and manufacturing keepouts.
# Stable identifier for this list entry.
- id: place_macros
  # Human-readable description for this manifest entry.
  description: 'Place macros for one candidate. Adapter target: OpenROAD macro placement.'
# Stable identifier for this list entry.
- id: set_macro_halo
  # Human-readable description for this manifest entry.
  description: Set the halo around one macro.
# Stable identifier for this list entry.
- id: set_macro_orientation
  # Human-readable description for this manifest entry.
  description: Set a legal orientation for one macro.
# Stable identifier for this list entry.
- id: set_macro_channel
  # Human-readable description for this manifest entry.
  description: Set the channel width between two macros.
# Stable identifier for this list entry.
- id: check_macro_overlaps
  # Human-readable description for this manifest entry.
  description: Check macros for overlap.
# Stable identifier for this list entry.
- id: check_macro_channels
  # Human-readable description for this manifest entry.
  description: Check channel widths against the routing requirement.
# Stable identifier for this list entry.
- id: check_macro_keepouts
  # Human-readable description for this manifest entry.
  description: Check that no macro sits in a keepout.
# Stable identifier for this list entry.
- id: score_macro_connectivity
  # Human-readable description for this manifest entry.
  description: Score flyline length and macro-to-macro connectivity.
# Stable identifier for this list entry.
- id: write_floorplan_def
  # Human-readable description for this manifest entry.
  description: Write the floorplan DEF for this candidate.
# Stable identifier for this list entry.
- id: generate_macro_placement
  # Human-readable description for this manifest entry.
  description: Place macros and write the candidate in one step when the hypothesis is a full placement.
# Stable identifier for this list entry.
- id: score_macro_placement
  # Human-readable description for this manifest entry.
  description: Score halo, channel, orientation, and connectivity.
# Stable identifier for this list entry.
- id: flag_keepout_violation
  # Human-readable description for this manifest entry.
  description: Publish a macro that entered a keepout.
# Stable identifier for this list entry.
- id: record_macro_placement_metrics
  # Human-readable description for this manifest entry.
  description: Record utilization, flylines, and the OpenROAD recipe id.
# Concrete Python callables the agent may invoke.
skills:
# Stable identifier for this list entry.
- id: initialize_floorplan
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: initialize_floorplan
  # Human-readable description for this manifest entry.
  description: 'Create the die, core, rows, and sites for this candidate. Adapter target: OpenROAD initialize_floorplan.'
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
# Stable identifier for this list entry.
- id: read_macro_inventory
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: read_macro_inventory
  # Human-readable description for this manifest entry.
  description: Read macros, abstracts, and orientation limits.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Specific report URI; empty selects latest for the candidate.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: read_reports
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate whose reports are read.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: report_ref
    # Human-readable description for this manifest entry.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Mode filter. Empty reads every available mode.
# Stable identifier for this list entry.
- id: read_floorplan_keepouts
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: read_floorplan_keepouts
  # Human-readable description for this manifest entry.
  description: Read blockages, halos, and manufacturing keepouts.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Specific report URI; empty selects latest for the candidate.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: read_reports
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate whose reports are read.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: report_ref
    # Human-readable description for this manifest entry.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Mode filter. Empty reads every available mode.
# Stable identifier for this list entry.
- id: place_macros
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: place_macros
  # Human-readable description for this manifest entry.
  description: 'Place macros for one candidate. Adapter target: OpenROAD macro placement.'
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
# Stable identifier for this list entry.
- id: set_macro_halo
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: set_macro_halo
  # Human-readable description for this manifest entry.
  description: Set the halo around one macro.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
    # Field `macro_name` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    macro_name: ''
    # Field `halo_microns` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    halo_microns: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: macro_name
    # Human-readable description for this manifest entry.
    description: Macro to update.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: halo_microns
    # Human-readable description for this manifest entry.
    description: Halo on each side, in microns.
# Stable identifier for this list entry.
- id: set_macro_orientation
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: set_macro_orientation
  # Human-readable description for this manifest entry.
  description: Set a legal orientation for one macro.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
    # Field `macro_name` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    macro_name: ''
    # Field `orientation` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    orientation: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: macro_name
    # Human-readable description for this manifest entry.
    description: Macro to orient.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: orientation
    # Human-readable description for this manifest entry.
    description: Legal orientation code.
# Stable identifier for this list entry.
- id: set_macro_channel
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: set_macro_channel
  # Human-readable description for this manifest entry.
  description: Set the channel width between two macros.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
# Stable identifier for this list entry.
- id: check_macro_overlaps
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: check_macro_overlaps
  # Human-readable description for this manifest entry.
  description: Check macros for overlap.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Field `recipe` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: submit_tool_job
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate the job reads.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Baseline used when the job compares against a known result.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: recipe
    # Human-readable description for this manifest entry.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: PVT corner. Empty when the job is not corner-specific.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Stable identifier for this list entry.
- id: check_macro_channels
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: check_macro_channels
  # Human-readable description for this manifest entry.
  description: Check channel widths against the routing requirement.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Field `recipe` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: submit_tool_job
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate the job reads.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Baseline used when the job compares against a known result.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: recipe
    # Human-readable description for this manifest entry.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: PVT corner. Empty when the job is not corner-specific.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Stable identifier for this list entry.
- id: check_macro_keepouts
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: check_macro_keepouts
  # Human-readable description for this manifest entry.
  description: Check that no macro sits in a keepout.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Field `recipe` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: submit_tool_job
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate the job reads.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Baseline used when the job compares against a known result.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: recipe
    # Human-readable description for this manifest entry.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: PVT corner. Empty when the job is not corner-specific.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Stable identifier for this list entry.
- id: score_macro_connectivity
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: score_macro_connectivity
  # Human-readable description for this manifest entry.
  description: Score flyline length and macro-to-macro connectivity.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Specific report URI; empty selects latest for the candidate.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: read_reports
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate whose reports are read.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: report_ref
    # Human-readable description for this manifest entry.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Mode filter. Empty reads every available mode.
# Stable identifier for this list entry.
- id: write_floorplan_def
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: write_floorplan_def
  # Human-readable description for this manifest entry.
  description: Write the floorplan DEF for this candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
# Stable identifier for this list entry.
- id: generate_macro_placement
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: generate_macro_placement
  # Human-readable description for this manifest entry.
  description: Place macros and write the candidate in one step when the hypothesis is a full placement.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Field `baseline_ref` in the Macro-Placement Experiment Worker agent manifest (floorplan stage, role worker).
    baseline_ref: ''
    # Single synthesis/floorplan knob changed in an isolated experiment.
    hypothesis: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: write_candidate
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: baseline_ref
    # Human-readable description for this manifest entry.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: hypothesis
    # Human-readable description for this manifest entry.
    description: The single change this edit is testing.
# Stable identifier for this list entry.
- id: score_macro_placement
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: score_macro_placement
  # Human-readable description for this manifest entry.
  description: Score halo, channel, orientation, and connectivity.
  # Default argument values when the planner omits them.
  args_defaults:
    # Candidate artifact reference for report reads.
    candidate_ref: ''
    # Specific report URI; empty selects latest for the candidate.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Operating-mode filter for report reads; empty means every mode.
    mode: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: read_reports
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: candidate_ref
    # Human-readable description for this manifest entry.
    description: Candidate whose reports are read.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: report_ref
    # Human-readable description for this manifest entry.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: corner
    # Human-readable description for this manifest entry.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: mode
    # Human-readable description for this manifest entry.
    description: Mode filter. Empty reads every available mode.
# Stable identifier for this list entry.
- id: flag_keepout_violation
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: flag_keepout_violation
  # Human-readable description for this manifest entry.
  description: Publish a macro that entered a keepout.
  # Default argument values when the planner omits them.
  args_defaults:
    # One-sentence finding text for publish_finding skills.
    summary: ''
    # Comma-separated artifact URIs supporting a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a finding.
    recommended_recipient: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: publish_finding
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: summary
    # Human-readable description for this manifest entry.
    description: One-sentence finding.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: evidence_refs
    # Human-readable description for this manifest entry.
    description: Comma-separated artifact URIs that support the finding.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: severity
    # Human-readable description for this manifest entry.
    description: low, medium, high, or critical.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: recommended_recipient
    # Human-readable description for this manifest entry.
    description: Agent id that should act on the finding.
# Stable identifier for this list entry.
- id: record_macro_placement_metrics
  # Python module (tools) exporting the callable.
  module: tools
  # Function name inside the module.
  callable: record_macro_placement_metrics
  # Human-readable description for this manifest entry.
  description: Record utilization, flylines, and the OpenROAD recipe id.
  # Default argument values when the planner omits them.
  args_defaults:
    # One-sentence finding text for publish_finding skills.
    summary: ''
    # Comma-separated artifact URIs supporting a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a finding.
    recommended_recipient: ''
  # Semantic ToolAction (create_workflow, read_reports, publish_finding, ...).
  action: publish_finding
  # Parameter map or documented skill parameter list for this entry.
  params:
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: summary
    # Human-readable description for this manifest entry.
    description: One-sentence finding.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: evidence_refs
    # Human-readable description for this manifest entry.
    description: Comma-separated artifact URIs that support the finding.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: severity
    # Human-readable description for this manifest entry.
    description: low, medium, high, or critical.
  # Stable agent id used in delegation graphs, findings, and telemetry.
  - name: recommended_recipient
    # Human-readable description for this manifest entry.
    description: Agent id that should act on the finding.
# Local journal cache configuration for this agent process.
memory:
  # Named memory stores the agent may touch.
  stores:
  # Stable identifier for this list entry.
  - id: journal
    # Human-readable description for this manifest entry.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Backend adapter type (none until OpenROAD/Yosys/etc. is wired).
    type: list
    # Persistence medium (file-backed under SILICON_MEMORY_ROOT).
    persistence: file
    # Store-specific configuration map.
    config:
      # Filesystem root for the local journal cache.
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry emission settings.
observability:
  # Seconds between liveness heartbeats.
  heartbeat_interval_secs: 5
  # Where structured telemetry streams are POSTed.
  telemetry:
    # Telemetry collector URL (TELEMETRY_URL override).
    endpoint: ${TELEMETRY_URL}
    # Named telemetry streams this agent emits.
    streams:
    # Stable agent id used in delegation graphs, findings, and telemetry.
    - name: skill_calls
      # Human-readable description for this manifest entry.
      description: Tool invocations.
    # Stable agent id used in delegation graphs, findings, and telemetry.
    - name: artifacts
      # Human-readable description for this manifest entry.
      description: Structured task results.
# Container image and runtime environment for this agent.
deployment:
  # Docker image tag published for this agent.
  image: agentfleet/macro_placement:1.0
  # Relative Dockerfile path used to build the image.
  dockerfile: Dockerfile
  # Environment variables injected at container start.
  environment:
    # Optional bound EDA framework name; empty keeps tools not_run.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Context-assembly policy for engineering memory records.
context:
  # Record kinds always pulled into the task context package.
  include:
  # Entry under `include` for Macro-Placement Experiment Worker: canonical_source
  - canonical_source
  # Entry under `include` for Macro-Placement Experiment Worker: open_findings
  - open_findings
  # Entry under `include` for Macro-Placement Experiment Worker: experiments
  - experiments
  # Entry under `include` for Macro-Placement Experiment Worker: gates
  - gates
  # Entry under `include` for Macro-Placement Experiment Worker: decisions
  - decisions
  # Record kinds omitted from context even if present.
  exclude:
  # Entry under `exclude` for Macro-Placement Experiment Worker: stale_candidates
  - stale_candidates
  # Validation states stripped from context (e.g. rejected).
  drop:
  # Entry under `drop` for Macro-Placement Experiment Worker: rejected
  - rejected
  # Preference order when conflicting records exist.
  precedence:
  # Entry under `precedence` for Macro-Placement Experiment Worker: human-authored
  - human-authored
  # Entry under `precedence` for Macro-Placement Experiment Worker: validated
  - validated
  # Entry under `precedence` for Macro-Placement Experiment Worker: provisional
  - provisional
  # Record authorship classes that must not be overwritten lightly.
  protect:
  # Entry under `protect` for Macro-Placement Experiment Worker: human-authored
  - human-authored
  # Approximate token budget for assembled context sent to the model.
  token_budget: 80000
