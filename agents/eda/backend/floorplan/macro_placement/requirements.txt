# Python dependencies for the Macro-Placement Experiment Worker agent HTTP process.
# These support the FastAPI AgentService surface; EDA engines are not pip packages here.

# fastapi: ASGI web framework exposing /health and /tasks/execute for this agent.
fastapi
# uvicorn[standard]: production ASGI server with extras (uvloop/httptools/watchfiles).
uvicorn[standard]
# pydantic: request/response and EDAAgentConfig-related data validation models.
pydantic
# PyYAML: loads config.yaml Agent manifests into EDAAgentConfig structures.
PyYAML
