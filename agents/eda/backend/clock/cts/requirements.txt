# FastAPI: ASGI web framework used by AgentService to expose /health and /tasks/execute.
fastapi
# Uvicorn with standard extras (websockets, httptools) to run the FastAPI app in production/dev.
uvicorn[standard]
# Pydantic: request/response and EDAAgentConfig-related models used across the EDA agent SDK.
pydantic
# PyYAML: parse this agent's config.yaml into the EDAAgentConfig consumed by EDAAgent.load_config.
PyYAML
