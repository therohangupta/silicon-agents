# Fleet manifest for the DFT Physical/Timing agent (dft_physical_timing).
# Parsed by EDAAgent.read_spec / gateway YAML scanner. Comments are documentation only.

# Fleet schema version for the agent manifest parser.
apiVersion: agentfleet/v1
# Resource kind; must be Agent for gateway YAML scanning.
kind: Agent
# Human- and machine-readable identity for dashboards and fleet select.
metadata:
  # Stable agent id used in delegates_to, tool agent_id, and routing.
  name: dft_physical_timing
  # UI label shown in the dashboard and logs.
  display_name: DFT Physical/Timing Coordination Agent
  # One-paragraph charter for planners and operators.
  description: Carry scan ordering, test clocks, congestion, and test-mode timing into physical design.
  # Stage and role for this agent.
  labels:
    # Pipeline stage this agent owns or serves.
    stage: dft
    # Role class: lead, specialist, or validator.
    role: worker
# Role class: lead, specialist, or validator.
role: worker
# Pipeline stage this agent owns or serves.
stage: dft
# Normative statement of what this agent owes the program.
responsibility: Carry scan ordering, test clocks, congestion, and test-mode timing into physical design.
# Hard may / may_not envelope enforced in planning and reviews.
boundary:
  # Actions this agent is allowed to take on candidates and memory.
  may:
  # List entry consumed by the dft_physical_timing manifest: Read scan and placement data
  - Read scan and placement data
  # List entry consumed by the dft_physical_timing manifest: Write a scan-order and test-mode constraint proposal
  - Write a scan-order and test-mode constraint proposal
  # List entry consumed by the dft_physical_timing manifest: Publish test-mode timing and congestion findings
  - Publish test-mode timing and congestion findings
  # Forbidden actions (waivers, baseline promotion, self-signoff, etc.).
  may_not:
  # List entry consumed by the dft_physical_timing manifest: Reorder scan in the canonical database
  - Reorder scan in the canonical database
  # List entry consumed by the dft_physical_timing manifest: Waive a test-mode timing violation
  - Waive a test-mode timing violation
  # List entry consumed by the dft_physical_timing manifest: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # List entry consumed by the dft_physical_timing manifest: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # List entry consumed by the dft_physical_timing manifest: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Child agent ids this lead may open workflows for.
delegates_to: []
# Independent gate agents that must grade this agent's output.
validators: []
# How other services reach this agent's HTTP process.
connection:
  # Hostname from sibling containers (Docker Desktop bridge).
  host: host.docker.internal
  # TCP port exposed by this agent's Dockerfile / server.
  port: 8230
  # HTTP paths the AgentService registers.
  endpoints:
    # Liveness probe path for orchestrators.
    health: /health
    # Task execution POST path used by the fleet server.
    execute: /tasks/execute
# EDA tool backend binding; none until a framework adapter is attached.
backend:
  # Backend adapter type (none means tool_observation returns not_run).
  type: none
# How tools are invoked inside the agent process.
execution:
  # Functional or test-mode filter for report reads.
  mode: direct_function
  # Parallelism limits for in-flight tasks.
  concurrency:
    # Maximum simultaneous tasks for this specialist.
    max_tasks: 1
# Timeouts and retry policy for failed DFT/lab work.
reliability:
  # Wall-clock seconds before a task is abandoned.
  task_timeout_secs: 120
  # Automatic retries before on_failure policy applies.
  max_retries: 1
  # Planner reaction when the specialist fails (e.g. replan).
  on_failure: replan
# Advertised capability ids for planners (may mirror skills).
capabilities:
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_scan_order
  # One-paragraph charter for planners and operators.
  description: Read the current scan-chain order.
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_test_mode_constraints
  # One-paragraph charter for planners and operators.
  description: Read test clocks, false paths, and case analysis for test mode.
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_scan_congestion
  # One-paragraph charter for planners and operators.
  description: Read congestion along scan nets.
# Manifest field '- id' for dft_physical_timing (dft).
- id: propose_scan_chain_order
  # One-paragraph charter for planners and operators.
  description: Write a proposed scan order that respects congestion and timing. This does not edit the
    canonical database.
# Manifest field '- id' for dft_physical_timing (dft).
- id: write_test_mode_sdc
  # One-paragraph charter for planners and operators.
  description: Write test-mode clocks and exceptions as a constraint candidate.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_test_mode_timing
  # One-paragraph charter for planners and operators.
  description: Report test-mode setup, hold, and transition violations.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_test_mode_power
  # One-paragraph charter for planners and operators.
  description: Report shift and capture power in the physical database.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_scan_routing_resource
  # One-paragraph charter for planners and operators.
  description: Report scan nets that cross congested regions.
# Manifest field '- id' for dft_physical_timing (dft).
- id: coordinate_scan_ordering
  # One-paragraph charter for planners and operators.
  description: Publish the proposed order and the congestion and timing evidence.
# Manifest field '- id' for dft_physical_timing (dft).
- id: flag_test_mode_timing_violation
  # One-paragraph charter for planners and operators.
  description: Publish a test-mode timing violation. This does not waive it.
# Manifest field '- id' for dft_physical_timing (dft).
- id: flag_scan_congestion
  # One-paragraph charter for planners and operators.
  description: Publish scan nets that need a physical change.
# Manifest field '- id' for dft_physical_timing (dft).
- id: record_dft_physical_handoff
  # One-paragraph charter for planners and operators.
  description: Record the scan order, test-mode SDC, and database revision handed to physical design.
# Concrete tool bindings: module, callable, action, params.
skills:
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_scan_order
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_scan_order
  # One-paragraph charter for planners and operators.
  description: Read the current scan-chain order.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_test_mode_constraints
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_test_mode_constraints
  # One-paragraph charter for planners and operators.
  description: Read test clocks, false paths, and case analysis for test mode.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_physical_timing (dft).
- id: read_scan_congestion
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_scan_congestion
  # One-paragraph charter for planners and operators.
  description: Read congestion along scan nets.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_physical_timing (dft).
- id: propose_scan_chain_order
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: propose_scan_chain_order
  # One-paragraph charter for planners and operators.
  description: Write a proposed scan order that respects congestion and timing. This does not edit the
    canonical database.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_physical_timing (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for dft_physical_timing (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for dft_physical_timing (dft).
- id: write_test_mode_sdc
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_test_mode_sdc
  # One-paragraph charter for planners and operators.
  description: Write test-mode clocks and exceptions as a constraint candidate.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_physical_timing (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for dft_physical_timing (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_test_mode_timing
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: check_test_mode_timing
  # One-paragraph charter for planners and operators.
  description: Report test-mode setup, hold, and transition violations.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_physical_timing (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_physical_timing (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_test_mode_power
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: check_test_mode_power
  # One-paragraph charter for planners and operators.
  description: Report shift and capture power in the physical database.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_physical_timing (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_physical_timing (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_physical_timing (dft).
- id: check_scan_routing_resource
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: check_scan_routing_resource
  # One-paragraph charter for planners and operators.
  description: Report scan nets that cross congested regions.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_physical_timing (dft).
- id: coordinate_scan_ordering
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: coordinate_scan_ordering
  # One-paragraph charter for planners and operators.
  description: Publish the proposed order and the congestion and timing evidence.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for dft_physical_timing (dft).
- id: flag_test_mode_timing_violation
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_test_mode_timing_violation
  # One-paragraph charter for planners and operators.
  description: Publish a test-mode timing violation. This does not waive it.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for dft_physical_timing (dft).
- id: flag_scan_congestion
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_scan_congestion
  # One-paragraph charter for planners and operators.
  description: Publish scan nets that need a physical change.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for dft_physical_timing (dft).
- id: record_dft_physical_handoff
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: record_dft_physical_handoff
  # One-paragraph charter for planners and operators.
  description: Record the scan order, test-mode SDC, and database revision handed to physical design.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_physical_timing (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Local and shared engineering-memory configuration.
memory:
  # Named memory stores this agent may touch.
  stores:
  # Manifest field '- id' for dft_physical_timing (dft).
  - id: journal
    # One-paragraph charter for planners and operators.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Backend adapter type (none means tool_observation returns not_run).
    type: list
    # Where the store is kept (file vs ephemeral).
    persistence: file
    # Store-specific configuration block.
    config:
      # Filesystem root for silicon memory (env-substitutable).
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry stream configuration.
observability:
  # Seconds between liveness heartbeats.
  heartbeat_interval_secs: 5
  # Where skill_calls and artifacts are ingested.
  telemetry:
    # Telemetry service base URL (TELEMETRY_URL).
    endpoint: ${TELEMETRY_URL}
    # Named high-level telemetry stream categories.
    streams:
    # Manifest field '- name' for dft_physical_timing (dft).
    - name: skill_calls
      # One-paragraph charter for planners and operators.
      description: Tool invocations.
    # Manifest field '- name' for dft_physical_timing (dft).
    - name: artifacts
      # One-paragraph charter for planners and operators.
      description: Structured task results.
# Container image and Dockerfile pointers for compose.
deployment:
  # Published image tag for this agent.
  image: agentfleet/dft_physical_timing:1.0
  # Relative Dockerfile path used to build the image.
  dockerfile: Dockerfile
  # Env vars injected into the container.
  environment:
    # Optional bound EDA/lab framework adapter name.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Context assembly policy for engineering memory.
context:
  # Record kinds always preferred into the context package.
  include:
  # List entry consumed by the dft_physical_timing manifest: requirements
  - requirements
  # List entry consumed by the dft_physical_timing manifest: canonical_source
  - canonical_source
  # List entry consumed by the dft_physical_timing manifest: open_findings
  - open_findings
  # List entry consumed by the dft_physical_timing manifest: gates
  - gates
  # Record kinds filtered out before ranking.
  exclude:
  # List entry consumed by the dft_physical_timing manifest: stale_candidates
  - stale_candidates
  # Validation states removed before context ranking.
  drop:
  # List entry consumed by the dft_physical_timing manifest: rejected
  - rejected
  # Most-trusted authorship/validation states first.
  precedence:
  # List entry consumed by the dft_physical_timing manifest: human-authored
  - human-authored
  # List entry consumed by the dft_physical_timing manifest: validated
  - validated
  # List entry consumed by the dft_physical_timing manifest: provisional
  - provisional
  # States never dropped solely for token budget.
  protect:
  # List entry consumed by the dft_physical_timing manifest: human-authored
  - human-authored
  # Approximate token cap for the assembled context package.
  token_budget: 80000
