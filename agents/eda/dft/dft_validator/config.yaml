# Fleet manifest for the DFT Validator agent (dft_validator).
# Parsed by EDAAgent.read_spec / gateway YAML scanner. Comments are documentation only.

# Fleet schema version for the agent manifest parser.
apiVersion: agentfleet/v1
# Resource kind; must be Agent for gateway YAML scanning.
kind: Agent
# Human- and machine-readable identity for dashboards and fleet select.
metadata:
  # Stable agent id used in delegates_to, tool agent_id, and routing.
  name: dft_validator
  # UI label shown in the dashboard and logs.
  display_name: Independent DFT Validator
  # One-paragraph charter for planners and operators.
  description: Reproduce scan, fault-coverage, self-test, and test-mode checks and decide whether the
    DFT contract passes.
  # Stage and role for this agent.
  labels:
    # Pipeline stage this agent owns or serves.
    stage: dft
    # Role class: lead, specialist, or validator.
    role: validator
# Role class: lead, specialist, or validator.
role: validator
# Pipeline stage this agent owns or serves.
stage: dft
# Normative statement of what this agent owes the program.
responsibility: Reproduce scan, fault-coverage, self-test, and test-mode checks and decide whether the
  DFT contract passes.
# Hard may / may_not envelope enforced in planning and reviews.
boundary:
  # Actions this agent is allowed to take on candidates and memory.
  may:
  # List entry consumed by the dft_validator manifest: Re-run critical DFT checks
  - Re-run critical DFT checks
  # List entry consumed by the dft_validator manifest: Read primary DFT reports
  - Read primary DFT reports
  # List entry consumed by the dft_validator manifest: Emit a gate
  - Emit a gate
  # Forbidden actions (waivers, baseline promotion, self-signoff, etc.).
  may_not:
  # List entry consumed by the dft_validator manifest: Edit scan or patterns
  - Edit scan or patterns
  # List entry consumed by the dft_validator manifest: Waive a coverage miss
  - Waive a coverage miss
  # List entry consumed by the dft_validator manifest: Accept a summary in place of the report
  - Accept a summary in place of the report
  # List entry consumed by the dft_validator manifest: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # List entry consumed by the dft_validator manifest: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # List entry consumed by the dft_validator manifest: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Child agent ids this lead may open workflows for.
delegates_to: []
# Independent gate agents that must grade this agent's output.
validators: []
# How other services reach this agent's HTTP process.
connection:
  # Hostname from sibling containers (Docker Desktop bridge).
  host: host.docker.internal
  # TCP port exposed by this agent's Dockerfile / server.
  port: 8231
  # HTTP paths the AgentService registers.
  endpoints:
    # Liveness probe path for orchestrators.
    health: /health
    # Task execution POST path used by the fleet server.
    execute: /tasks/execute
# EDA tool backend binding; none until a framework adapter is attached.
backend:
  # Backend adapter type (none means tool_observation returns not_run).
  type: none
# How tools are invoked inside the agent process.
execution:
  # Functional or test-mode filter for report reads.
  mode: direct_function
  # Parallelism limits for in-flight tasks.
  concurrency:
    # Maximum simultaneous tasks for this specialist.
    max_tasks: 1
# Timeouts and retry policy for failed DFT/lab work.
reliability:
  # Wall-clock seconds before a task is abandoned.
  task_timeout_secs: 120
  # Automatic retries before on_failure policy applies.
  max_retries: 1
  # Planner reaction when the specialist fails (e.g. replan).
  on_failure: replan
# Advertised capability ids for planners (may mirror skills).
capabilities:
# Manifest field '- id' for dft_validator (dft).
- id: read_dft_candidate_pin
  # One-paragraph charter for planners and operators.
  description: Read the exact candidate and pattern set being graded.
# Manifest field '- id' for dft_validator (dft).
- id: read_primary_dft_reports
  # One-paragraph charter for planners and operators.
  description: Read scan, ATPG, BIST, and test-mode reports from primary artifacts.
# Manifest field '- id' for dft_validator (dft).
- id: check_dft_provenance
  # One-paragraph charter for planners and operators.
  description: Check DFT tool versions and recipes against the frozen flow.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_scan_integrity
  # One-paragraph charter for planners and operators.
  description: Re-run scan connectivity and chain-integrity checks.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Re-run fault coverage for every required model.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_bist_checks
  # One-paragraph charter for planners and operators.
  description: Re-run MBIST and LBIST integration checks.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_test_mode_timing
  # One-paragraph charter for planners and operators.
  description: Re-run test-mode timing on the physical database.
# Manifest field '- id' for dft_validator (dft).
- id: compare_dft_summary
  # One-paragraph charter for planners and operators.
  description: Compare the DFT lead's summary with the primary reports.
# Manifest field '- id' for dft_validator (dft).
- id: audit_dft_untestables
  # One-paragraph charter for planners and operators.
  description: Check that every untestable fault has a traced cause.
# Manifest field '- id' for dft_validator (dft).
- id: emit_dft_gate
  # One-paragraph charter for planners and operators.
  description: Record whether the DFT contract passed.
# Manifest field '- id' for dft_validator (dft).
- id: publish_dft_gate_disagreement
  # One-paragraph charter for planners and operators.
  description: Publish a place where the summary and the primary report disagree.
# Manifest field '- id' for dft_validator (dft).
- id: publish_missing_dft_input
  # One-paragraph charter for planners and operators.
  description: Publish a report or fault model required for the gate that is absent.
# Concrete tool bindings: module, callable, action, params.
skills:
# Manifest field '- id' for dft_validator (dft).
- id: read_dft_candidate_pin
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_dft_candidate_pin
  # One-paragraph charter for planners and operators.
  description: Read the exact candidate and pattern set being graded.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_validator (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_validator (dft).
- id: read_primary_dft_reports
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_primary_dft_reports
  # One-paragraph charter for planners and operators.
  description: Read scan, ATPG, BIST, and test-mode reports from primary artifacts.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_validator (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_validator (dft).
- id: check_dft_provenance
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: check_dft_provenance
  # One-paragraph charter for planners and operators.
  description: Check DFT tool versions and recipes against the frozen flow.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_validator (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_scan_integrity
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: reproduce_scan_integrity
  # One-paragraph charter for planners and operators.
  description: Re-run scan connectivity and chain-integrity checks.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_validator (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_validator (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_validator (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_validator (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_fault_coverage
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: reproduce_fault_coverage
  # One-paragraph charter for planners and operators.
  description: Re-run fault coverage for every required model.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_validator (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_validator (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_validator (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_validator (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_bist_checks
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: reproduce_bist_checks
  # One-paragraph charter for planners and operators.
  description: Re-run MBIST and LBIST integration checks.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_validator (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_validator (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_validator (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_validator (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_validator (dft).
- id: reproduce_test_mode_timing
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: reproduce_test_mode_timing
  # One-paragraph charter for planners and operators.
  description: Re-run test-mode timing on the physical database.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for dft_validator (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for dft_validator (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for dft_validator (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for dft_validator (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for dft_validator (dft).
- id: compare_dft_summary
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: compare_dft_summary
  # One-paragraph charter for planners and operators.
  description: Compare the DFT lead's summary with the primary reports.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_validator (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_validator (dft).
- id: audit_dft_untestables
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: audit_dft_untestables
  # One-paragraph charter for planners and operators.
  description: Check that every untestable fault has a traced cause.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for dft_validator (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for dft_validator (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for dft_validator (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for dft_validator (dft).
- id: emit_dft_gate
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: emit_dft_gate
  # One-paragraph charter for planners and operators.
  description: Record whether the DFT contract passed.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'gate_id' for dft_validator (dft).
    gate_id: ''
    # Manifest field 'check_refs' for dft_validator (dft).
    check_refs: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: emit_gate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Exact candidate being graded.
  # Manifest field '- name' for dft_validator (dft).
  - name: gate_id
    # One-paragraph charter for planners and operators.
    description: Stable id for this gate decision.
  # Manifest field '- name' for dft_validator (dft).
  - name: check_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated primary report URIs the decision is based on.
# Manifest field '- id' for dft_validator (dft).
- id: publish_dft_gate_disagreement
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: publish_dft_gate_disagreement
  # One-paragraph charter for planners and operators.
  description: Publish a place where the summary and the primary report disagree.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_validator (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_validator (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_validator (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for dft_validator (dft).
- id: publish_missing_dft_input
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: publish_missing_dft_input
  # One-paragraph charter for planners and operators.
  description: Publish a report or fault model required for the gate that is absent.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for dft_validator (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for dft_validator (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for dft_validator (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for dft_validator (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Local and shared engineering-memory configuration.
memory:
  # Named memory stores this agent may touch.
  stores:
  # Manifest field '- id' for dft_validator (dft).
  - id: journal
    # One-paragraph charter for planners and operators.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Backend adapter type (none means tool_observation returns not_run).
    type: list
    # Where the store is kept (file vs ephemeral).
    persistence: file
    # Store-specific configuration block.
    config:
      # Filesystem root for silicon memory (env-substitutable).
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry stream configuration.
observability:
  # Seconds between liveness heartbeats.
  heartbeat_interval_secs: 5
  # Where skill_calls and artifacts are ingested.
  telemetry:
    # Telemetry service base URL (TELEMETRY_URL).
    endpoint: ${TELEMETRY_URL}
    # Named high-level telemetry stream categories.
    streams:
    # Manifest field '- name' for dft_validator (dft).
    - name: skill_calls
      # One-paragraph charter for planners and operators.
      description: Tool invocations.
    # Manifest field '- name' for dft_validator (dft).
    - name: artifacts
      # One-paragraph charter for planners and operators.
      description: Structured task results.
# Container image and Dockerfile pointers for compose.
deployment:
  # Published image tag for this agent.
  image: agentfleet/dft_validator:1.0
  # Relative Dockerfile path used to build the image.
  dockerfile: Dockerfile
  # Env vars injected into the container.
  environment:
    # Optional bound EDA/lab framework adapter name.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Context assembly policy for engineering memory.
context:
  # Record kinds always preferred into the context package.
  include:
  # List entry consumed by the dft_validator manifest: requirements
  - requirements
  # List entry consumed by the dft_validator manifest: interface_contracts
  - interface_contracts
  # List entry consumed by the dft_validator manifest: canonical_source
  - canonical_source
  # List entry consumed by the dft_validator manifest: gates
  - gates
  # List entry consumed by the dft_validator manifest: open_findings
  - open_findings
  # Record kinds filtered out before ranking.
  exclude:
  # List entry consumed by the dft_validator manifest: stale_candidates
  - stale_candidates
  # List entry consumed by the dft_validator manifest: unverified_agent_claims
  - unverified_agent_claims
  # Validation states removed before context ranking.
  drop:
  # List entry consumed by the dft_validator manifest: rejected
  - rejected
  # Most-trusted authorship/validation states first.
  precedence:
  # List entry consumed by the dft_validator manifest: human-authored
  - human-authored
  # List entry consumed by the dft_validator manifest: validated
  - validated
  # States never dropped solely for token budget.
  protect:
  # List entry consumed by the dft_validator manifest: human-authored
  - human-authored
  # Approximate token cap for the assembled context package.
  token_budget: 80000
