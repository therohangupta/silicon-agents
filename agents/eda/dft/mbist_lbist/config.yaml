# Fleet manifest for the MBIST/LBIST agent (mbist_lbist).
# Parsed by EDAAgent.read_spec / gateway YAML scanner. Comments are documentation only.

# Fleet schema version for the agent manifest parser.
apiVersion: agentfleet/v1
# Resource kind; must be Agent for gateway YAML scanning.
kind: Agent
# Human- and machine-readable identity for dashboards and fleet select.
metadata:
  # Stable agent id used in delegates_to, tool agent_id, and routing.
  name: mbist_lbist
  # UI label shown in the dashboard and logs.
  display_name: MBIST/LBIST Agent
  # One-paragraph charter for planners and operators.
  description: Configure memory and logic BIST controllers, algorithms, repair, and diagnostic modes.
  # Stage and role for this agent.
  labels:
    # Pipeline stage this agent owns or serves.
    stage: dft
    # Role class: lead, specialist, or validator.
    role: worker
# Role class: lead, specialist, or validator.
role: worker
# Pipeline stage this agent owns or serves.
stage: dft
# Normative statement of what this agent owes the program.
responsibility: Configure memory and logic BIST controllers, algorithms, repair, and diagnostic modes.
# Hard may / may_not envelope enforced in planning and reviews.
boundary:
  # Actions this agent is allowed to take on candidates and memory.
  may:
  # List entry consumed by the mbist_lbist manifest: Edit BIST configuration on an isolated candidate
  - Edit BIST configuration on an isolated candidate
  # List entry consumed by the mbist_lbist manifest: Submit a BIST integration check
  - Submit a BIST integration check
  # Forbidden actions (waivers, baseline promotion, self-signoff, etc.).
  may_not:
  # List entry consumed by the mbist_lbist manifest: Change the memory map
  - Change the memory map
  # List entry consumed by the mbist_lbist manifest: Claim coverage without a BIST report
  - Claim coverage without a BIST report
  # List entry consumed by the mbist_lbist manifest: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # List entry consumed by the mbist_lbist manifest: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # List entry consumed by the mbist_lbist manifest: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Child agent ids this lead may open workflows for.
delegates_to: []
# Independent gate agents that must grade this agent's output.
validators: []
# How other services reach this agent's HTTP process.
connection:
  # Hostname from sibling containers (Docker Desktop bridge).
  host: host.docker.internal
  # TCP port exposed by this agent's Dockerfile / server.
  port: 8229
  # HTTP paths the AgentService registers.
  endpoints:
    # Liveness probe path for orchestrators.
    health: /health
    # Task execution POST path used by the fleet server.
    execute: /tasks/execute
# EDA tool backend binding; none until a framework adapter is attached.
backend:
  # Backend adapter type (none means tool_observation returns not_run).
  type: none
# How tools are invoked inside the agent process.
execution:
  # Functional or test-mode filter for report reads.
  mode: direct_function
  # Parallelism limits for in-flight tasks.
  concurrency:
    # Maximum simultaneous tasks for this specialist.
    max_tasks: 1
# Timeouts and retry policy for failed DFT/lab work.
reliability:
  # Wall-clock seconds before a task is abandoned.
  task_timeout_secs: 120
  # Automatic retries before on_failure policy applies.
  max_retries: 1
  # Planner reaction when the specialist fails (e.g. replan).
  on_failure: replan
# Advertised capability ids for planners (may mirror skills).
capabilities:
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_controller
  # One-paragraph charter for planners and operators.
  description: Configure the MBIST controller for the memories in scope.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_algorithm
  # One-paragraph charter for planners and operators.
  description: Select and write the March algorithm for each memory.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_memory_repair_interface
  # One-paragraph charter for planners and operators.
  description: Write the repair interface and redundancy map.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_diagnostic_mode
  # One-paragraph charter for planners and operators.
  description: Write the diagnostic mode that reports failing address and bit.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_lbist_controller
  # One-paragraph charter for planners and operators.
  description: Configure the LBIST controller, PRPG, and MISR.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_lbist_seed
  # One-paragraph charter for planners and operators.
  description: Write the LBIST seed set.
# Manifest field '- id' for mbist_lbist (dft).
- id: bind_bist_to_memories
  # One-paragraph charter for planners and operators.
  description: Bind controllers to the memory instances in the candidate.
# Manifest field '- id' for mbist_lbist (dft).
- id: check_bist_integration
  # One-paragraph charter for planners and operators.
  description: Check that BIST ports, clocks, and resets integrate with the functional design.
# Manifest field '- id' for mbist_lbist (dft).
- id: read_bist_coverage
  # One-paragraph charter for planners and operators.
  description: Read MBIST and LBIST coverage from the BIST report.
# Manifest field '- id' for mbist_lbist (dft).
- id: read_bist_timing_constraints
  # One-paragraph charter for planners and operators.
  description: Read the test-mode clocks and false paths BIST requires.
# Manifest field '- id' for mbist_lbist (dft).
- id: flag_uncovered_memory
  # One-paragraph charter for planners and operators.
  description: Publish a memory with no MBIST algorithm.
# Manifest field '- id' for mbist_lbist (dft).
- id: flag_bist_integration_error
  # One-paragraph charter for planners and operators.
  description: Publish a BIST port, clock, or reset that does not integrate.
# Manifest field '- id' for mbist_lbist (dft).
- id: record_bist_configuration
  # One-paragraph charter for planners and operators.
  description: Record algorithms, seeds, and the candidate they were applied to.
# Concrete tool bindings: module, callable, action, params.
skills:
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_controller
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_mbist_controller
  # One-paragraph charter for planners and operators.
  description: Configure the MBIST controller for the memories in scope.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_algorithm
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_mbist_algorithm
  # One-paragraph charter for planners and operators.
  description: Select and write the March algorithm for each memory.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
    # Manifest field 'memory_id' for mbist_lbist (dft).
    memory_id: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: memory_id
    # One-paragraph charter for planners and operators.
    description: Memory the algorithm targets.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_memory_repair_interface
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_memory_repair_interface
  # One-paragraph charter for planners and operators.
  description: Write the repair interface and redundancy map.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_mbist_diagnostic_mode
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_mbist_diagnostic_mode
  # One-paragraph charter for planners and operators.
  description: Write the diagnostic mode that reports failing address and bit.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_lbist_controller
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_lbist_controller
  # One-paragraph charter for planners and operators.
  description: Configure the LBIST controller, PRPG, and MISR.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: write_lbist_seed
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: write_lbist_seed
  # One-paragraph charter for planners and operators.
  description: Write the LBIST seed set.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: bind_bist_to_memories
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: bind_bist_to_memories
  # One-paragraph charter for planners and operators.
  description: Bind controllers to the memory instances in the candidate.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'hypothesis' for mbist_lbist (dft).
    hypothesis: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: write_candidate
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Immutable baseline the candidate must descend from.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: hypothesis
    # One-paragraph charter for planners and operators.
    description: The single change this edit is testing.
# Manifest field '- id' for mbist_lbist (dft).
- id: check_bist_integration
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: check_bist_integration
  # One-paragraph charter for planners and operators.
  description: Check that BIST ports, clocks, and resets integrate with the functional design.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Manifest field 'baseline_ref' for mbist_lbist (dft).
    baseline_ref: ''
    # Manifest field 'recipe' for mbist_lbist (dft).
    recipe: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: submit_tool_job
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate the job reads.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: baseline_ref
    # One-paragraph charter for planners and operators.
    description: Baseline used when the job compares against a known result.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: recipe
    # One-paragraph charter for planners and operators.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: PVT corner. Empty when the job is not corner-specific.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Manifest field '- id' for mbist_lbist (dft).
- id: read_bist_coverage
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_bist_coverage
  # One-paragraph charter for planners and operators.
  description: Read MBIST and LBIST coverage from the BIST report.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for mbist_lbist (dft).
- id: read_bist_timing_constraints
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: read_bist_timing_constraints
  # One-paragraph charter for planners and operators.
  description: Read the test-mode clocks and false paths BIST requires.
  # Default argument values when the LLM omits them.
  args_defaults:
    # Artifact URI of the RTL/netlist/physical candidate under test.
    candidate_ref: ''
    # Artifact URI of a DFT or lab report; empty means latest.
    report_ref: ''
    # PVT corner filter for report reads.
    corner: ''
    # Functional or test-mode filter for report reads.
    mode: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: read_reports
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: candidate_ref
    # One-paragraph charter for planners and operators.
    description: Candidate whose reports are read.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: report_ref
    # One-paragraph charter for planners and operators.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: corner
    # One-paragraph charter for planners and operators.
    description: Corner filter. Empty reads every available corner.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: mode
    # One-paragraph charter for planners and operators.
    description: Mode filter. Empty reads every available mode.
# Manifest field '- id' for mbist_lbist (dft).
- id: flag_uncovered_memory
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_uncovered_memory
  # One-paragraph charter for planners and operators.
  description: Publish a memory with no MBIST algorithm.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for mbist_lbist (dft).
- id: flag_bist_integration_error
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: flag_bist_integration_error
  # One-paragraph charter for planners and operators.
  description: Publish a BIST port, clock, or reset that does not integrate.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Manifest field '- id' for mbist_lbist (dft).
- id: record_bist_configuration
  # Python module (relative) that defines the callable.
  module: tools
  # Function name exported from tools.py.
  callable: record_bist_configuration
  # One-paragraph charter for planners and operators.
  description: Record algorithms, seeds, and the candidate they were applied to.
  # Default argument values when the LLM omits them.
  args_defaults:
    # One-sentence finding text published to engineering memory.
    summary: ''
    # Comma-separated artifact URIs backing a finding.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act on a published finding.
    recommended_recipient: ''
  # ToolAction enum semantic (create_workflow, read_reports, …).
  action: publish_finding
  # Documented parameters the planner/LLM may fill.
  params:
  # Manifest field '- name' for mbist_lbist (dft).
  - name: summary
    # One-paragraph charter for planners and operators.
    description: One-sentence finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: evidence_refs
    # One-paragraph charter for planners and operators.
    description: Comma-separated artifact URIs that support the finding.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: severity
    # One-paragraph charter for planners and operators.
    description: low, medium, high, or critical.
  # Manifest field '- name' for mbist_lbist (dft).
  - name: recommended_recipient
    # One-paragraph charter for planners and operators.
    description: Agent id that should act on the finding.
# Local and shared engineering-memory configuration.
memory:
  # Named memory stores this agent may touch.
  stores:
  # Manifest field '- id' for mbist_lbist (dft).
  - id: journal
    # One-paragraph charter for planners and operators.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Backend adapter type (none means tool_observation returns not_run).
    type: list
    # Where the store is kept (file vs ephemeral).
    persistence: file
    # Store-specific configuration block.
    config:
      # Filesystem root for silicon memory (env-substitutable).
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry stream configuration.
observability:
  # Seconds between liveness heartbeats.
  heartbeat_interval_secs: 5
  # Where skill_calls and artifacts are ingested.
  telemetry:
    # Telemetry service base URL (TELEMETRY_URL).
    endpoint: ${TELEMETRY_URL}
    # Named high-level telemetry stream categories.
    streams:
    # Manifest field '- name' for mbist_lbist (dft).
    - name: skill_calls
      # One-paragraph charter for planners and operators.
      description: Tool invocations.
    # Manifest field '- name' for mbist_lbist (dft).
    - name: artifacts
      # One-paragraph charter for planners and operators.
      description: Structured task results.
# Container image and Dockerfile pointers for compose.
deployment:
  # Published image tag for this agent.
  image: agentfleet/mbist_lbist:1.0
  # Relative Dockerfile path used to build the image.
  dockerfile: Dockerfile
  # Env vars injected into the container.
  environment:
    # Optional bound EDA/lab framework adapter name.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Context assembly policy for engineering memory.
context:
  # Record kinds always preferred into the context package.
  include:
  # List entry consumed by the mbist_lbist manifest: requirements
  - requirements
  # List entry consumed by the mbist_lbist manifest: canonical_source
  - canonical_source
  # List entry consumed by the mbist_lbist manifest: open_findings
  - open_findings
  # List entry consumed by the mbist_lbist manifest: gates
  - gates
  # Record kinds filtered out before ranking.
  exclude:
  # List entry consumed by the mbist_lbist manifest: stale_candidates
  - stale_candidates
  # Validation states removed before context ranking.
  drop:
  # List entry consumed by the mbist_lbist manifest: rejected
  - rejected
  # Most-trusted authorship/validation states first.
  precedence:
  # List entry consumed by the mbist_lbist manifest: human-authored
  - human-authored
  # List entry consumed by the mbist_lbist manifest: validated
  - validated
  # List entry consumed by the mbist_lbist manifest: provisional
  - provisional
  # States never dropped solely for token budget.
  protect:
  # List entry consumed by the mbist_lbist manifest: human-authored
  - human-authored
  # Approximate token cap for the assembled context package.
  token_budget: 80000
