# Agent specification for RTL Implementation Agent (`rtl_implementation`).
# Stage: rtl. Role: worker. HTTP port: 8209.
# RTL implementation makes one bounded source edit per candidate branch with an explicit hypothesis. Isolated branches keep the canonical baseline immutable until a lead promotes a qualified candidate.
# Every key below is loaded by EdaAgent.read_spec / AgentService at startup.
# Comments are documentation only; they do not change runtime behavior.

# Fleet manifest schema version; must match the agentfleet loader.
apiVersion: agentfleet/v1
# Resource kind. Agent means this YAML is an agent specification.
kind: Agent
# Human-facing identity and labels used by fleet discovery and dashboards.
metadata:
  # Stable agent id `rtl_implementation` used in routing and telemetry.
  name: rtl_implementation
  # Dashboard title for `rtl_implementation`.
  display_name: RTL Implementation Agent
  # Charter shown to planners and operators.
  description: Make one bounded source change in an isolated branch and record the hypothesis it is testing.
  # Labels expose stage and role.
  labels:
    # Lifecycle stage `rtl` for planner routing.
    stage: rtl
    # Agent role `worker` (lead vs worker) controlling delegation rights.
    role: worker
# Agent role `worker` (lead vs worker) controlling delegation rights.
role: worker
# Lifecycle stage `rtl` for planner routing.
stage: rtl
# Charter repeated at the top level for validators and planners.
responsibility: Make one bounded source change in an isolated branch and record the hypothesis it is testing.
# Hard may / may_not limits that prevent unsafe cross-stage edits.
boundary:
  # Actions this agent is allowed to perform on candidates and reports.
  may:
  # Allowed action: Create one candidate branch from a pinned baseline
  - Create one candidate branch from a pinned baseline
  # Allowed action: Edit RTL only on that branch
  - Edit RTL only on that branch
  # Actions forbidden even if a tool adapter could physically do them.
  may_not:
  # Forbidden action: Push to the canonical revision
  - Push to the canonical revision
  # Forbidden action: Declare the change functionally correct
  - Declare the change functionally correct
  # Forbidden action: Promote a candidate onto the canonical baseline
  - Promote a candidate onto the canonical baseline
  # Forbidden action: Approve a waiver or relax a hard requirement
  - Approve a waiver or relax a hard requirement
  # Forbidden action: Treat its own summary as independent signoff
  - Treat its own summary as independent signoff
# Worker agent ids this lead may open tasks against.
delegates_to: []
# Optional gate agents that must approve before promotion.
validators: []
# How peer services reach this agent's HTTP surface.
connection:
  # Docker host alias so containers reach the published port on the host.
  host: host.docker.internal
  # TCP port for this agent; must match Dockerfile EXPOSE (8209).
  port: 8209
  # Relative HTTP paths for health and task execution.
  endpoints:
    # Liveness HTTP path for compose/fleet probes.
    health: /health
    # Task execution HTTP path used by the orchestrator.
    execute: /tasks/execute
# Where tool jobs run; type none means stub observations until bound.
backend:
  # none = no live EDA backend; tools return not_run stubs.
  type: none
# How the runtime invokes skills for this agent.
execution:
  # direct_function invokes Python skills in-process.
  mode: direct_function
  # Limits parallel task load on this container.
  concurrency:
    # Serialise tasks when 1 so candidate edits do not race.
    max_tasks: 1
# Timeouts and retry policy for failed tool or LLM turns.
reliability:
  # Abort tasks that exceed 120 seconds.
  task_timeout_secs: 120
  # Retry budget `1` before giving up / replanning.
  max_retries: 1
  # Planner policy `replan` after task failure.
  on_failure: replan
# Advertised skill ids the planner may schedule.
capabilities:
# Identifier `create_candidate_branch` under `capabilities` for `rtl_implementation`.
- id: create_candidate_branch
  # One-line charter of what this agent owns in the chip flow.
  description: Create an isolated worktree from the pinned baseline.
# Identifier `apply_bounded_rtl_edit` under `capabilities` for `rtl_implementation`.
- id: apply_bounded_rtl_edit
  # One-line charter of what this agent owns in the chip flow.
  description: Apply one RTL edit that matches the task hypothesis.
# Identifier `add_module_port` under `capabilities` for `rtl_implementation`.
- id: add_module_port
  # One-line charter of what this agent owns in the chip flow.
  description: Add one port required by an interface contract.
# Identifier `rename_signal` under `capabilities` for `rtl_implementation`.
- id: rename_signal
  # One-line charter of what this agent owns in the chip flow.
  description: Rename one signal and its references inside the candidate.
# Identifier `insert_pipeline_stage` under `capabilities` for `rtl_implementation`.
- id: insert_pipeline_stage
  # One-line charter of what this agent owns in the chip flow.
  description: Add one pipeline stage on a named path inside the candidate.
# Identifier `register_output` under `capabilities` for `rtl_implementation`.
- id: register_output
  # One-line charter of what this agent owns in the chip flow.
  description: Register one combinational output in the candidate.
# Identifier `write_file_header` under `capabilities` for `rtl_implementation`.
- id: write_file_header
  # One-line charter of what this agent owns in the chip flow.
  description: Write the candidate's change note, hypothesis, and baseline ref into the touched files.
# Identifier `summarize_candidate_diff` under `capabilities` for `rtl_implementation`.
- id: summarize_candidate_diff
  # One-line charter of what this agent owns in the chip flow.
  description: Summarize the diff, the hypothesis, and the files touched.
# Identifier `list_candidate_files` under `capabilities` for `rtl_implementation`.
- id: list_candidate_files
  # One-line charter of what this agent owns in the chip flow.
  description: List files this candidate changed relative to the baseline.
# Identifier `read_module_ports` under `capabilities` for `rtl_implementation`.
- id: read_module_ports
  # One-line charter of what this agent owns in the chip flow.
  description: Read the ports of one module in the candidate.
# Identifier `compile_candidate` under `capabilities` for `rtl_implementation`.
- id: compile_candidate
  # One-line charter of what this agent owns in the chip flow.
  description: Compile or elaborate the candidate and return syntax and elaboration errors.
# Identifier `record_edit_hypothesis` under `capabilities` for `rtl_implementation`.
- id: record_edit_hypothesis
  # One-line charter of what this agent owns in the chip flow.
  description: Publish the hypothesis, diff stat, and files for this candidate.
# Binding of capability ids to Python callables and action kinds.
skills:
# Identifier `create_candidate_branch` under `skills` for `rtl_implementation`.
- id: create_candidate_branch
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: create_candidate_branch
  # One-line charter of what this agent owns in the chip flow.
  description: Create an isolated worktree from the pinned baseline.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
# Identifier `apply_bounded_rtl_edit` under `skills` for `rtl_implementation`.
- id: apply_bounded_rtl_edit
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: apply_bounded_rtl_edit
  # One-line charter of what this agent owns in the chip flow.
  description: Apply one RTL edit that matches the task hypothesis.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
    # Path inside the candidate tree relative to the candidate root.
    file_path: ''
    # Field `edit_summary` = `''` for agent `rtl_implementation`.
    edit_summary: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: file_path
    # One-line charter of what this agent owns in the chip flow.
    description: RTL file to edit.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: edit_summary
    # One-line charter of what this agent owns in the chip flow.
    description: What the edit changes.
# Identifier `add_module_port` under `skills` for `rtl_implementation`.
- id: add_module_port
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: add_module_port
  # One-line charter of what this agent owns in the chip flow.
  description: Add one port required by an interface contract.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
    # Module path exporting the skill callable (always `tools` here).
    module: ''
    # TCP port for this agent; must match Dockerfile EXPOSE ('').
    port: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: module
    # One-line charter of what this agent owns in the chip flow.
    description: Module to edit.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: port
    # One-line charter of what this agent owns in the chip flow.
    description: Port declaration.
# Identifier `rename_signal` under `skills` for `rtl_implementation`.
- id: rename_signal
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: rename_signal
  # One-line charter of what this agent owns in the chip flow.
  description: Rename one signal and its references inside the candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
# Identifier `insert_pipeline_stage` under `skills` for `rtl_implementation`.
- id: insert_pipeline_stage
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: insert_pipeline_stage
  # One-line charter of what this agent owns in the chip flow.
  description: Add one pipeline stage on a named path inside the candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
    # Field `path_group` = `''` for agent `rtl_implementation`.
    path_group: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: path_group
    # One-line charter of what this agent owns in the chip flow.
    description: Logic the stage cuts.
# Identifier `register_output` under `skills` for `rtl_implementation`.
- id: register_output
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: register_output
  # One-line charter of what this agent owns in the chip flow.
  description: Register one combinational output in the candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
# Identifier `write_file_header` under `skills` for `rtl_implementation`.
- id: write_file_header
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: write_file_header
  # One-line charter of what this agent owns in the chip flow.
  description: Write the candidate's change note, hypothesis, and baseline ref into the touched files.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Single change under test for this edit so experiments stay one-hypothesis-at-a-time.
    hypothesis: ''
  # Mutates only an isolated candidate branch, never the canonical baseline.
  action: write_candidate
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Isolated candidate to edit. Empty means create one from the baseline.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Immutable baseline the candidate must descend from.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: hypothesis
    # One-line charter of what this agent owns in the chip flow.
    description: The single change this edit is testing.
# Identifier `summarize_candidate_diff` under `skills` for `rtl_implementation`.
- id: summarize_candidate_diff
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: summarize_candidate_diff
  # One-line charter of what this agent owns in the chip flow.
  description: Summarize the diff, the hypothesis, and the files touched.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Artifact URI of a prior report. Empty reads the latest report for this candidate.
    report_ref: ''
    # PVT corner (process/voltage/temperature). Empty when the job is not corner-specific.
    corner: ''
    # direct_function calls Python tools in-process rather than shelling out.
    mode: ''
  # Reads structured report artifacts without mutating the candidate.
  action: read_reports
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Candidate whose reports are read.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: report_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: corner
    # One-line charter of what this agent owns in the chip flow.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: mode
    # One-line charter of what this agent owns in the chip flow.
    description: Mode filter. Empty reads every available mode.
# Identifier `list_candidate_files` under `skills` for `rtl_implementation`.
- id: list_candidate_files
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: list_candidate_files
  # One-line charter of what this agent owns in the chip flow.
  description: List files this candidate changed relative to the baseline.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Artifact URI of a prior report. Empty reads the latest report for this candidate.
    report_ref: ''
    # PVT corner (process/voltage/temperature). Empty when the job is not corner-specific.
    corner: ''
    # direct_function calls Python tools in-process rather than shelling out.
    mode: ''
  # Reads structured report artifacts without mutating the candidate.
  action: read_reports
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Candidate whose reports are read.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: report_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: corner
    # One-line charter of what this agent owns in the chip flow.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: mode
    # One-line charter of what this agent owns in the chip flow.
    description: Mode filter. Empty reads every available mode.
# Identifier `read_module_ports` under `skills` for `rtl_implementation`.
- id: read_module_ports
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: read_module_ports
  # One-line charter of what this agent owns in the chip flow.
  description: Read the ports of one module in the candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Artifact URI of a prior report. Empty reads the latest report for this candidate.
    report_ref: ''
    # PVT corner (process/voltage/temperature). Empty when the job is not corner-specific.
    corner: ''
    # direct_function calls Python tools in-process rather than shelling out.
    mode: ''
    # Module path exporting the skill callable (always `tools` here).
    module: ''
  # Reads structured report artifacts without mutating the candidate.
  action: read_reports
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Candidate whose reports are read.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: report_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Artifact URI of the report. Empty reads the latest report for this candidate.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: corner
    # One-line charter of what this agent owns in the chip flow.
    description: Corner filter. Empty reads every available corner.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: mode
    # One-line charter of what this agent owns in the chip flow.
    description: Mode filter. Empty reads every available mode.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: module
    # One-line charter of what this agent owns in the chip flow.
    description: Module to read.
# Identifier `compile_candidate` under `skills` for `rtl_implementation`.
- id: compile_candidate
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: compile_candidate
  # One-line charter of what this agent owns in the chip flow.
  description: Compile or elaborate the candidate and return syntax and elaboration errors.
  # Default argument values when the planner omits them.
  args_defaults:
    # Artifact URI of the isolated design candidate this skill reads or edits; empty often means create-from-baseline.
    candidate_ref: ''
    # Immutable baseline revision the candidate must descend from so lineage stays auditable.
    baseline_ref: ''
    # Versioned EDA-tool recipe name; the framework adapter rejects unknown recipes.
    recipe: ''
    # PVT corner (process/voltage/temperature). Empty when the job is not corner-specific.
    corner: ''
    # direct_function calls Python tools in-process rather than shelling out.
    mode: ''
  # Queues an EDA tool job through the bound adapter (stubbed as not_run until bound).
  action: submit_tool_job
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: candidate_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Candidate the job reads.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: baseline_ref
    # One-line charter of what this agent owns in the chip flow.
    description: Baseline used when the job compares against a known result.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: recipe
    # One-line charter of what this agent owns in the chip flow.
    description: Versioned tool recipe. The adapter rejects an unknown recipe.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: corner
    # One-line charter of what this agent owns in the chip flow.
    description: PVT corner. Empty when the job is not corner-specific.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: mode
    # One-line charter of what this agent owns in the chip flow.
    description: Functional or analysis mode. Empty when the job is not mode-specific.
# Identifier `record_edit_hypothesis` under `skills` for `rtl_implementation`.
- id: record_edit_hypothesis
  # Module path exporting the skill callable (always `tools` here).
  module: tools
  # Python function in tools.py implementing this skill.
  callable: record_edit_hypothesis
  # One-line charter of what this agent owns in the chip flow.
  description: Publish the hypothesis, diff stat, and files for this candidate.
  # Default argument values when the planner omits them.
  args_defaults:
    # One-sentence finding text published into shared engineering memory.
    summary: ''
    # Comma-separated artifact URIs that back the finding for later audit.
    evidence_refs: ''
    # Finding severity: low, medium, high, or critical.
    severity: ''
    # Agent id that should act next on this finding (lead or specialist).
    recommended_recipient: ''
  # Publishes a finding into shared memory for leads and validators.
  action: publish_finding
  # Documented parameter list for UI and schema generation.
  params:
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: summary
    # One-line charter of what this agent owns in the chip flow.
    description: One-sentence finding.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: evidence_refs
    # One-line charter of what this agent owns in the chip flow.
    description: Comma-separated artifact URIs that support the finding.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: severity
    # One-line charter of what this agent owns in the chip flow.
    description: low, medium, high, or critical.
  # Stable agent id used in routing, telemetry, and delegate targets.
  - name: recommended_recipient
    # One-line charter of what this agent owns in the chip flow.
    description: Agent id that should act on the finding.
# Local memory store configuration for this agent process.
memory:
  # Named stores the agent may read/write through the memory API.
  stores:
  # Identifier `journal` under `stores` for `rtl_implementation`.
  - id: journal
    # One-line charter of what this agent owns in the chip flow.
    description: Local cache of task journal keys. Shared engineering memory is the silicon store.
    # Memory store type `list`.
    type: list
    # file means journal keys are cached on local disk.
    persistence: file
    # Store-specific configuration block.
    config:
      # Local journal cache root; override with SILICON_MEMORY_ROOT.
      path: ${SILICON_MEMORY_ROOT:-/tmp/silicon_memory}
# Heartbeat and telemetry wiring for fleet dashboards.
observability:
  # Seconds between liveness heartbeats to telemetry.
  heartbeat_interval_secs: 5
  # Where structured skill and artifact events are shipped.
  telemetry:
    # Telemetry base URL; override with TELEMETRY_URL.
    endpoint: ${TELEMETRY_URL}
    # Named event streams this agent emits.
    streams:
    # Stable agent id used in routing, telemetry, and delegate targets.
    - name: skill_calls
      # One-line charter of what this agent owns in the chip flow.
      description: Tool invocations.
    # Stable agent id used in routing, telemetry, and delegate targets.
    - name: artifacts
      # One-line charter of what this agent owns in the chip flow.
      description: Structured task results.
# Container image metadata used by compose generation.
deployment:
  # Container image tag built from this agent's Dockerfile.
  image: agentfleet/rtl_implementation:1.0
  # Dockerfile path relative to this agent directory.
  dockerfile: Dockerfile
  # Environment variables injected at container start.
  environment:
    # Empty keeps adapters unbound; set to enable real tool execution.
    EDA_FRAMEWORK: ${EDA_FRAMEWORK:-}
# Policy for which shared-memory records enter the LLM context window.
context:
  # Record kinds always considered for context assembly.
  include:
  # Context include kind: requirements (pulled into LLM context when present).
  - requirements
  # Context include kind: interface_contracts (pulled into LLM context when present).
  - interface_contracts
  # Context include kind: canonical_source (pulled into LLM context when present).
  - canonical_source
  # Context include kind: open_findings (pulled into LLM context when present).
  - open_findings
  # Context include kind: experiments (pulled into LLM context when present).
  - experiments
  # Record kinds never considered even if present.
  exclude:
  # Context exclude kind: stale_candidates (never assembled into prompts).
  - stale_candidates
  # States removed before ranking (e.g. rejected).
  drop:
  # Drop memory state before ranking: rejected.
  - rejected
  # Preferred record states first when trimming to token_budget.
  precedence:
  # Prefer memory state `human-authored` when trimming to token_budget.
  - human-authored
  # Prefer memory state `validated` when trimming to token_budget.
  - validated
  # Prefer memory state `provisional` when trimming to token_budget.
  - provisional
  # States kept even when over budget (usually human-authored).
  protect:
  # Never drop memory state `human-authored` even when over budget.
  - human-authored
  # Approximate max tokens of assembled context (80000).
  token_budget: 80000
