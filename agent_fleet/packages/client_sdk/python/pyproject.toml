[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-fleet-client-sdk"
version = "0.0.0"
description = "Python client SDK for agent-fleet gateway"
requires-python = ">=3.11"
dependencies = ["httpx", "websockets"]

